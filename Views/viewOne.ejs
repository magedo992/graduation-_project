<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= chemical.name %></title>
</head>
<body>
    <h1><%= chemical.name %></h1>
    <p>Type: <%= chemical.ChemicalsType %></p>
    <p>Active Ingredient: <%= chemical.activeingredient.name %> (<%= chemical.activeingredient.concentration %>%)</p>
    <p>Safety Period: <%= chemical.safetyPeriod %> days</p>
    <p>Price per KG: $<%= chemical.pricePerKG %></p>
    <h3>Images:</h3>
    <ul>
        <% chemical.images.forEach(image => { %>
            <li>
                <img src="<%= image.url %>" alt="<%= chemical.name %>" style="width: 200px;">
            </li>
        <% }) %>
    </ul>
    <a href="/viewChemicals">Back to List</a>
</body>
</html>
